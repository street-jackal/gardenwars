import { makeStyles, createStyles, Theme } from "@material-ui/core/styles";

const cardContainerStyles = makeStyles((theme: Theme) =>
  createStyles({
    main: {
      marginTop: 20,
    },
  })
);

export default cardContainerStyles;
